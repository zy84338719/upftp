# Test configuration file with HTTP auth enabled
port: "10000"
ftp_port: "2121"
root: "./test-share"
auto_select: false
enable_ftp: false
enable_mcp: false

# HTTP Basic Authentication
http_auth:
  enabled: true
  username: "testuser"
  password: "testpass123"

# Logging settings
logging:
  level: "debug"
  format: "text"

# Upload settings
upload:
  enabled: true
  max_size: 104857600
  allow_types: ""
